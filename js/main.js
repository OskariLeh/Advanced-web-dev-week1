documentBody = document.querySelector("body")

const createWikiItems =  () => {
    wikiItem = document.createElement("div")
    wikiItem.class
    wikiHeader = document.createElement("h1")
    wikiHeader.innerText = "Breed X"
    wikiItem.appendChild(wikiHeader)
    documentBody.appendChild(wikiItem)
}

createWikiItems()